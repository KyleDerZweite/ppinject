# Usage

`ppinject` does not yet provide a functional injection API.

This document exists to reserve the future usage surface for:

- opening a `.pptx` archive
- resolving slide targets
- selecting text-bearing XML nodes
- applying targeted XML-safe replacements
- validating output behavior in PowerPoint

When implementation begins, examples in this document should stay synthetic and avoid real customer presentation data.
