# Publishing

Publishing automation is intentionally deferred in the scaffold phase.

Before enabling package publication:

1. The library should provide a minimally usable PPTX mutation API.
2. CI should cover build, lint, type check, and test execution reliably.
3. Release notes should describe supported behavior and known limitations honestly.
4. PyPI workflow and trusted publisher configuration should be added together.
